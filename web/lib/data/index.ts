export {
  getAll,
  getById,
  remove,
  save,
  update,
} from './local-storage'

export {
  CURRENT_ADMIN_ID,
  CURRENT_STUDENT_ID,
  CURRENT_TEACHER_ID,
  getCurrentAdmin,
  getCurrentStudent,
  getCurrentStudentId,
  getCurrentTeacher,
  initializeData,
  initialAttempts,
  initialClasses,
  initialCourses,
  initialExamAssignments,
  initialExams,
  initialQuestions,
  initialSchedules,
  initialSubjects,
  initialSubmissions,
  initialUsers,
} from './seed-init'

export { initialResults } from './seed-results'
